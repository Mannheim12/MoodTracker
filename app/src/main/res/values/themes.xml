<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme for Compose -->
    <style name="Theme.MoodTracker" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Keep this for the mood selection screen -->
    <style name="Theme.MoodTracker.NoActionBar" parent="Theme.MoodTracker">
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>